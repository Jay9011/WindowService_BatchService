<UserControl x:Class="SettingsUI.Views.LoggingTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:res="clr-namespace:Utility.Resources;assembly=Utility"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="520">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="{x:Static res:Strings.LabelDefaultLogLevel}" VerticalAlignment="Center" />
        <ComboBox  Grid.Row="0" Grid.Column="1" Margin="0,4"
                   ItemsSource="{Binding LogLevelOptions}"
                   SelectedItem="{Binding Default}" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="{x:Static res:Strings.LabelHostingLifetimeLogLevel}" VerticalAlignment="Center" />
        <ComboBox  Grid.Row="1" Grid.Column="1" Margin="0,4"
                   ItemsSource="{Binding LogLevelOptions}"
                   SelectedItem="{Binding HostingLifetime}" />
    </Grid>
</UserControl>
